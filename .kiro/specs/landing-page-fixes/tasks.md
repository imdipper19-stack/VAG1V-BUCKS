# План реализации

- [ ] 1. Написать exploration-тест для bug condition (исчезновение пакетов и захардкоженные заказы)
  - **Property 1: Bug Condition** - Карточки пакетов исчезают при клике и заказы не загружаются из API
  - **CRITICAL**: Этот тест ДОЛЖЕН УПАСТЬ на нефиксированном коде — падение подтверждает наличие бага
  - **НЕ ПЫТАЙТЕСЬ исправить тест или код, когда он падает**
  - **NOTE**: Тест кодирует ожидаемое поведение — он подтвердит исправление, когда пройдёт после имплементации
  - **GOAL**: Обнаружить контрпримеры, демонстрирующие наличие багов
  - **Scoped PBT Approach**: Для Бага 2 — scope на конкретный случай: клик по карточке пакета после reveal-анимации. Для Бага 1 — проверить вызов `ordersApi.list`
  - Баг 2: Проверить, что после клика по карточке пакета (`setSelectedPackage`) все карточки НЕ содержат класс `landing-reveal` без `is-visible` (из Bug Condition: `isBugCondition_PackageDisappear(X)` где `X.action = "clickPackageCard" AND X.sectionAlreadyVisible = TRUE`)
  - Баг 1: Проверить, что при рендере компонента вызывается `ordersApi.list({ status: 'completed', limit: 4 })` (из Bug Condition: `isBugCondition_LiveOrders(X)` — баг проявляется всегда)
  - Ожидаемое поведение: карточки остаются видимыми после клика; секция заказов отображает данные из API
  - Запустить тест на НЕФИКСИРОВАННОМ коде
  - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тест ПАДАЕТ (это корректно — доказывает наличие бага)
  - Задокументировать найденные контрпримеры (например: «после `setSelectedPackage(800)` карточки теряют `is-visible`», «`ordersApi.list` никогда не вызывается»)
  - Отметить задачу выполненной когда тест написан, запущен и падение задокументировано
  - _Requirements: 1.1, 1.2, 1.3, 1.4_

- [ ] 2. Написать preservation property-тесты (ДО реализации исправления)
  - **Property 2: Preservation** - Загрузка пакетов, навигация и scroll-reveal не затронуты
  - **IMPORTANT**: Следовать observation-first методологии
  - Наблюдение: `pricingApi.list()` вызывается при загрузке и пакеты отображаются корректно на нефиксированном коде
  - Наблюдение: клик «Перейти к оплате» формирует URL `/payment?amount={vbucksAmount}&price={priceRUB}` на нефиксированном коде
  - Наблюдение: панель «К покупке выбран пакет» обновляется при смене `selectedPackage` на нефиксированном коде
  - Наблюдение: scroll-reveal анимация работает для секций hero, trust, FAQ, steps на нефиксированном коде
  - Написать property-based тест: для всех взаимодействий, НЕ связанных с bug condition, поведение компонента сохраняется (из Preservation Requirements в design)
  - Проверить, что `pricingApi.list()` продолжает вызываться независимо от загрузки заказов
  - Проверить, что навигация к оплате формирует корректный URL с выбранным пакетом
  - Проверить, что панель покупки обновляется при смене пакета
  - Запустить тесты на НЕФИКСИРОВАННОМ коде
  - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тесты ПРОХОДЯТ (подтверждает baseline-поведение для сохранения)
  - Отметить задачу выполненной когда тесты написаны, запущены и проходят на нефиксированном коде
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [ ] 3. Исправление багов лендинга

  - [ ] 3.1 Исправить Баг 2 — Убрать `landing-reveal` с карточек пакетов
    - В `frontend/src/app/page.tsx` убрать `landing-reveal` из className кнопки `<button>` в секции `landing-package-grid`
    - Было: `className={\`landing-pack landing-reveal ${pkg.popular || isSelected ? 'featured' : ''}\`}`
    - Стало: `className={\`landing-pack ${pkg.popular || isSelected ? 'featured' : ''}\`}`
    - Убрать `data-delay={String(index % 3)}` с кнопки (не имеет эффекта без `landing-reveal`)
    - _Bug_Condition: isBugCondition_PackageDisappear(X) где X.action = "clickPackageCard" AND X.sectionAlreadyVisible = TRUE_
    - _Expected_Behavior: секция остаётся видимой после клика, выбранный пакет выделяется классом `featured`_
    - _Preservation: scroll-reveal анимация секции-обёртки #packages продолжает работать_
    - _Requirements: 2.4, 2.5, 3.4_

  - [ ] 3.2 Исправить Баг 1 — Загрузить реальные завершённые заказы
    - Добавить `ordersApi` в импорт из `@/lib/api`
    - Добавить `useState` для `liveOrders`, инициализированный `LIVE_ORDERS`: `const [liveOrders, setLiveOrders] = useState(LIVE_ORDERS)`
    - Добавить helper-функцию `formatRelativeTime(dateStr: string): string` — вычисляет разницу между `now` и `dateStr`, возвращает строку вида «5 минут», «1 час», «2 дня»
    - В существующем `useEffect` (или новом) вызвать `ordersApi.list({ status: 'completed', limit: 4 })` и замапить ответ в формат отображения `[orderId, vbucks, relativeTime, status]`
    - Заменить `LIVE_ORDERS.map(...)` на `liveOrders.map(...)` в JSX секции `#live`
    - При ошибке API или пустом ответе — оставить fallback `LIVE_ORDERS`
    - _Bug_Condition: isBugCondition_LiveOrders(X) — баг проявляется всегда, API не вызывается_
    - _Expected_Behavior: секция отображает реальные данные из API (orderId, vbucks, relativeTime, статус «Выдано»)_
    - _Preservation: загрузка пакетов из pricingApi.list() не затронута, рендер лендинга не блокируется_
    - _Requirements: 2.1, 2.2, 2.3, 3.1, 3.2, 3.3_

  - [ ] 3.3 Проверить, что exploration-тест (Property 1) теперь проходит
    - **Property 1: Expected Behavior** - Карточки пакетов видимы после клика и заказы загружаются из API
    - **IMPORTANT**: Перезапустить ТОТ ЖЕ тест из задачи 1 — НЕ писать новый тест
    - Тест из задачи 1 кодирует ожидаемое поведение
    - Когда тест проходит — это подтверждает, что ожидаемое поведение достигнуто
    - Запустить exploration-тест из шага 1
    - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТ (подтверждает исправление бага)
    - _Requirements: 2.1, 2.4, 2.5_

  - [ ] 3.4 Проверить, что preservation-тесты всё ещё проходят
    - **Property 2: Preservation** - Загрузка пакетов, навигация и scroll-reveal не затронуты
    - **IMPORTANT**: Перезапустить ТЕ ЖЕ тесты из задачи 2 — НЕ писать новые тесты
    - Запустить preservation property-тесты из шага 2
    - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тесты ПРОХОДЯТ (подтверждает отсутствие регрессий)
    - Убедиться, что все тесты проходят после исправления (нет регрессий)

- [ ] 4. Checkpoint — Убедиться, что все тесты проходят
  - Запустить полный набор тестов проекта
  - Проверить, что фронтенд компилируется без ошибок
  - Убедиться, что exploration-тест (Property 1) проходит — баг исправлен
  - Убедиться, что preservation-тесты (Property 2) проходят — регрессий нет
  - При возникновении вопросов — спросить пользователя
